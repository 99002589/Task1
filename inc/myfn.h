#ifndef __MYFN_H__
#define __MYFN_H__

#include<stdio.h>
//#incluide<conio.h>
#include<stdlib.h>

int odd(int no);
int primeno(int no);
int symbol(int no);
int palindrome(int no);
int armstrong(int no);

#endif
